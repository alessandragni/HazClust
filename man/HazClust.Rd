% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/HazClust.R
\name{HazClust}
\alias{HazClust}
\title{HazClust algorithm}
\usage{
HazClust(
  formula,
  cluster = NULL,
  strata = NULL,
  data,
  inip = NULL,
  iniFpar = NULL,
  dist = c("weibull", "inweibull", "frechet", "exponential", "gompertz", "loglogistic",
    "lognormal", "logskewnormal"),
  frailty = c("none", "gamma", "ingau", "possta", "lognormal", "loglogistic"),
  method = "nlminb",
  maxitparfm = 500,
  maxit = 500,
  tolS = 1e-04,
  tolll = 0.001,
  Fparscale = 1,
  showtime = FALSE,
  correct = 0,
  transform = TRUE,
  c,
  k,
  gammapar,
  lambda0 = NULL,
  S0 = NULL,
  trace = TRUE
)
}
\arguments{
\item{formula}{A formula with a survival object created by \code{Surv()} on the left and
covariates on the right.}

\item{cluster}{Optional. The name (character string) of the variable in \code{data} indicating cluster IDs.}

\item{strata}{Optional. The name (character string) of the variable in \code{data} indicating strata.}

\item{data}{A data frame containing the variables in the model.}

\item{inip}{Optional. Initial values for the baseline hazard and regression parameters.}

\item{iniFpar}{Optional. Initial value(s) for the frailty distribution parameter(s).}

\item{dist}{A character string specifying the baseline hazard distribution.
One of: \code{"weibull"}, \code{"inweibull"}, \code{"frechet"},
\code{"exponential"}, \code{"gompertz"}, \code{"loglogistic"},
\code{"lognormal"}, \code{"logskewnormal"}.}

\item{frailty}{A character string specifying the frailty distribution.
One of: \code{"none"}, \code{"gamma"}, \code{"ingau"},
\code{"possta"}, \code{"lognormal"}.}

\item{method}{Optimization method passed to \code{optimx}.}

\item{maxitparfm}{Maximum number of iterations for internal optimization.}

\item{maxit}{Maximum number of iterations for optimization.}

\item{tolS}{Convergence tolerance for S.}

\item{tolll}{Convergence tolerance for log-likelihood.}

\item{Fparscale}{Scaling value for the frailty parameter in optimisation.
The algorithm optimizes \code{Fpar / Fparscale}.}

\item{showtime}{Logical. Should the execution time be printed?}

\item{correct}{Used only with \code{frailty = "possta"}. Correction factor for likelihood
calculation in cases of many events per cluster.}

\item{transform}{Logical. Whether to apply transformation during distance computation.}

\item{c}{Integer. Number of clusters.}

\item{k}{Integer. Number of nearest neighbors used in the similarity matrix.}

\item{gammapar}{Parameter for l1 penalization on the similarity matrix.}

\item{lambda0}{Optional. Initial value for the lambda penalty term.}

\item{S0}{Optional. Initial similarity matrix. Defaults to uniform.}

\item{trace}{Logical. Should progress and convergence information be printed?}
}
\value{
A list containing:
\item{estimate}{Named vector of parameter estimates (frailty, baseline hazard, regression).}
\item{F}{Low-rank embedding matrix computed from Laplacian eigenvectors.}
\item{S}{Estimated similarity matrix.}
\item{loglik}{Log-likelihood value at the optimum.}
\item{iterations}{Number of optimization iterations.}
\item{convergence}{Logical. Whether the algorithm converged.}
\item{lambda}{Final value of the penalty parameter lambda.}
\item{time}{Execution time, if \code{showtime = TRUE}.}
}
\description{
HazClust algorithm
}
\details{
The function uses maximum likelihood estimation to fit a parametric frailty model.
If \code{frailty = "none"}, then the model is fitted without random effects.
Stratification allows different baseline hazards across strata.
}
\examples{
\dontrun{
data(kidney, package = "survival")
parfm(Surv(time, status) ~ age + sex, cluster = "id", data = kidney,
      dist = "weibull", frailty = "gamma")
}

}
